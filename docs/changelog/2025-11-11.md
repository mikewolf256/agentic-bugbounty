export OUTPUT_DIR=./output_zap
export MAX_REQ_PER_SEC=3
export PYTHONPATH=.
# only if your ZAP has a key:
export ZAP_API_BASE='http://127.0.0.1:8080'
export ZAP_API_KEY='changeme'


3) Start ZAP spider + active scan (primary)
SCAN_JSON=$(curl -s -X POST http://localhost:8100/mcp/start_zap_scan \
  -H 'Content-Type: application/json' \
  -d '{"targets":["you.23andme.com","auth.23andme.com","api.23andme.com","store.23andme.com"]}')
echo "$SCAN_JSON" | jq .
SCAN_ID=$(echo "$SCAN_JSON" | jq -r '.our_scan_id')
