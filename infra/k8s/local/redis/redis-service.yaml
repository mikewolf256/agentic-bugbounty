apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: scan-workers
  labels:
    app: redis
    component: queue
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
  selector:
    app: redis

