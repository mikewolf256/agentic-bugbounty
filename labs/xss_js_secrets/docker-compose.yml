services:
  xss_js_secrets:
    build: .
    ports:
      - "5001:5000"
    networks:
      - lab_network

networks:
  lab_network:
    # Use external network when running from main docker-compose
    # Falls back to creating local network when run standalone
    name: agentic-bugbounty_lab_network
    external: false
